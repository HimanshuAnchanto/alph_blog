<!--
<style>
table,thead,td,tr,tbody,th{
        background-color: lightgrey;
        color: rgb(19, 129, 78);
        border: 2.5px solid rgb(143, 22, 22);
        padding: 15px;
        text-align: center;
        border-spacing: 10px;
        }

        th
        {

            padding-top: 12px;
            padding-bottom: 12px;
            text-align: left;
            background-color: #04AA6D;
            color: white;
        }
        tr:hover {background-color: #ddd;}}
    </style> %>
<h2>Articles Index Page</h2>

<table> 
<thead>
<tr>
<th>Title</th>
<th>description</th>
<th colspan=3 style="text-align:center">Actions</th>
</tr>
</thead>

<tbody>
<% @articles.each do |article_record| %>
<tr>
<td> <%= article_record.title %></td>
<td> <%= article_record.description %></td>
<td><%= link_to 'show', article_path(article_record)%></td>
<td><%= link_to 'edit', edit_article_path(article_record) %> </td>
<td><%= link_to 'delete', article_path(article_record), method: :delete, data: { confirm: "Are you sure to delete?" } %></td>
</tr>
<% end %>
</tbody> 

</table>
<br>
<button type="button" class="btn btn-primary">Primary</button> 
<p class="btn btn-primary"  style="background-color:black; font-family:verdana; margin-left:15px;" ><%= link_to 'Create new Article', new_article_path %> </p>

-->
<h1 class= "text-center mt-4">Listing All articles</h1>
<div class="container">
   <% @articles.each do |article| %>
     <div class="row justify-content-md-center">
       <div class="col-8 mt-4">
         <div class="card text-center shadow mb-5 bg-white rounded">
           <div class="card-header font-italic">
             by <%= article.user.username %>
           </div>
           <div class="card-body">
             <h5 class="card-title"><%= link_to article.title, article_path(article), class: "text-success" %></h5>
             <p class="card-text"><%= truncate(article.description, length: 100) %></p>
             <%= link_to "View", article_path(article), class: "btn btn-outline-success" %>
             <%= link_to "Edit", edit_article_path(article), class: "btn btn-outline-info" %>
             <%= link_to "Delete", article_path(article), class: "btn btn-outline-danger", method: :delete, data: {confirm: "Are you sure?"} %>
           </div>
           <div class="card-footer text-muted">
             <small>Created <%= time_ago_in_words(article.created_at) %> ago, 
             edited <%= time_ago_in_words(article.updated_at) %> ago</small>
           </div>
         </div>
       </div>
     </div>
   <% end %>
   </div>
 </div



